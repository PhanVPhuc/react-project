const initialState = {
  list: ["Nghe nhac khong"],
  activeId: null,
};

const hobbyReducer = (state = initialState, action) => {
  switch (action.type) {
    // case : ADD_HOBBY

    default:
      return state;
  }
};

export default hobbyReducer;
